import { time } from "console"

export interface UserModel{
  id:number,
  userId:number,
  name:string,
  email:string,
  companyId:number,
  userAddedAt:string,
  userIsActive:boolean,
  userMailValue:string,
}
